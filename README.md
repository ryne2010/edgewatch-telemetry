# edgewatch-telemetry
A local-first edge-to-cloud telemetry and alerting system with buffered device ingestion, online/offline health detection, Postgres-backed time-series storage, a tiny read-only dashboard, configurable notification routing (SMTP/Slack/SMS stub), and Cloud Run–friendly deployment patterns.
