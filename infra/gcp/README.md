# GCP infrastructure

- `cloud_run_demo/` — deploys EdgeWatch API + UI to Cloud Run
- `modules/` — reusable Terraform baseline modules

Deploy via repo root Makefile:

```bash
make deploy-gcp
```

See `docs/DEPLOY_GCP.md`.
